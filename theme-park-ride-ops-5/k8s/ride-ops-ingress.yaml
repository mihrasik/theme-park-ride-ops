apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ride-ops-ingress
  namespace: themepark-app
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
  - host: ride-ops.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ride-ops
            port:
              number: 8080