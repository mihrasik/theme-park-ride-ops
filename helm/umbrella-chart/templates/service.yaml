apiVersion: v1
kind: Service
metadata:
  name: {{ include "umbrella-chart.fullname" . }}
  labels:
    app: {{ include "umbrella-chart.name" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: {{ include "umbrella-chart.name" . }}